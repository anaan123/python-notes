# Python 函数参数与常见易错点（前端转 Python）

> 面向对象：**有前端背景（JS / Vue），转学 Python 的初学者**
> 核心目标：**彻底看懂函数参数，避免 90% 新手坑**

---

## 🎯 学习目标

* 看懂 Python 函数参数
* 不混淆 `args / kwargs / tuple / list / dict / set`
* 理解「参数接收」和「函数逻辑」的区别

---

## 一、函数参数基础

### 1️⃣ 位置参数（Positional Arguments）

```python
def add(a, b):
    return a + b

add(1, 2)
```

特点：

* 按**顺序**传参
* 与 JS 普通函数参数一致

---

### 2️⃣ 默认参数（Default Arguments）

```python
def create_user(name, role="user", active=True):
    return {
        "name": name,
        "role": role,
        "active": active
    }

create_user("张三")
create_user("李四", "admin", False)
```

规则：

* 有默认值的参数可以不传
* **默认参数必须放在普通参数后面**

---

### 3️⃣ 关键字参数（Keyword Arguments）

```python
create_user(name="王五", active=False)
```

特点：

* 调用时显式指定参数名
* 顺序可以打乱

---

## 二、`*args`：任意数量的位置参数

### 基本用法

```python
def sum_all(*args):
    return sum(args)

sum_all(1, 2, 3, 4)  # 10
```

### 关键结论 ⭐

* `args` 的类型是 **tuple（元组）**
* `args` **永远是 tuple**

```python
def demo(*args):
    print(type(args))  # <class 'tuple'>
```

> tuple 特点：**有序、不可变**

---

## 三、`**kwargs`：任意数量的关键字参数

### 基本用法

```python
def print_info(**kwargs):
    for key, value in kwargs.items():
        print(f"{key}: {value}")

print_info(name="张三", age=25, city="北京")
```

### 关键结论 ⭐

* `kwargs` 的类型是 **dict**
* `kwargs` 本质就是字典
* **key 一定是字符串**

---

## 四、`*args` vs `**kwargs`（直观对比）

```python
def demo(*args, **kwargs):
    print(args)
    print(kwargs)


demo(1, 2, 3, name="张三", age=18)
```

输出：

```text
(1, 2, 3)
{'name': '张三', 'age': 18}
```

📌 说明：

* `args`：只是**接收参数**，不会自动计算
* 不会自动变成 `sum(args)`

---

## 五、`print` vs `return`（新手高频误区）

### ❌ 只有 `print`，函数返回值是 `None`

```python
def demo(*args):
    print(sum(args))

result = demo(1, 2, 3)
print(result)  # None
```

### ✅ 使用 `return` 才能拿到结果

```python
def demo(*args):
    return sum(args)

print(demo(1, 2, 3))  # 6
```

### 重要结论 ⭐

* `print`：给**人**看的
* `return`：给**程序**用的

---

## 六、函数可以返回多个值（本质是 tuple）

```python
def demo(*args, **kwargs):
    return sum(args), kwargs

result = demo(1, 2, 3, name="李四")
print(result)
```

等价于：

```python
(6, {'name': '李四'})
```

📌 **多个返回值 = 一个 tuple**

---

## 七、tuple / list / set / dict 终极防混淆

### 1️⃣ tuple（元组）

```python
t = 1,
t = (1,)
```

* **看逗号**
* 与括号关系不大

---

### 2️⃣ list（列表）

```python
[1, 2, 3]
```

* 中括号
* 可变

---

### 3️⃣ set（集合）

```python
{1, 2, 3}
```

* 花括号
* **无冒号**
* 去重、无序

---

### 4️⃣ dict（字典）

```python
{"a": 1, "b": 2}
```

* 花括号 + 冒号

---

## 八、终极防混淆口诀（必背）

* ⭐ tuple 看逗号（但前提不是 `{}` 和 `[]`）
* ⭐ 花括号永远不会创建 tuple
* ⭐ `()` + `,` → tuple
* ⭐ `{}` + 无冒号 → set
* ⭐ `{}` + 冒号 → dict

---

## 九、常见陷阱示例

```python
t = (1)
type(t)  # int ❌


t = (1,)
type(t)  # tuple ✅
```

```python
{1, 2: 3}  # ❌ 非法（set 和 dict 混用）
```

---

## 十、命名习惯说明（不是关键字）

| 名称      | 含义                       |
| ------- | ------------------------ |
| args    | arguments（参数）            |
| kwargs  | keyword arguments（关键字参数） |
| options | 配置选项                     |
| config  | 配置对象                     |

📌 `args / kwargs` **不是关键字**，可以改名，但不推荐。

---

## 十一、同名函数会被覆盖（容易忽略）

```python
def demo():
    pass


def demo():
    pass
```

📌 **后定义的会覆盖前面的**

---

## ⭐ 总结一句话

> **参数怎么进函数 ≠ 函数怎么处理参数**
> `*args / **kwargs` 只是「接收方式」，不是「运算逻辑」
